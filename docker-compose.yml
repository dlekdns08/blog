services:
  blog:
    build: .
    ports:
      - "3000:3000"
    environment:
      - CLAUDE_TOKEN=${CLAUDE_TOKEN}
      - ARXIV_DB_PATH=/data/arxiv_graph.db
    volumes:
      - /app/actions-runner-arxiv/_work/arxiv-graph/arxiv-graph/data:/data:ro
    restart: always